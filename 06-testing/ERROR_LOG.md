# Error Log

| Date | Workflow | Error | Cause | Fix | Lesson |
|---|---|---|---|---|---|
| 2026-09-01 | Milo Outline Generator v0.1 | Controlled `CANON_LINEAGE_INVALID` for historical `MILO-004` while governed `MILO-007` was ready | `Read Eligible Stories` used `status=CONCEPT_APPROVED` with first-match enabled, allowing an earlier PRE-CANON LEGACY row to starve a legitimate governed Story | Read and deterministically classify the complete candidate set, exclude PRE-CANON LEGACY rows, select one governed Story, and route malformed candidates through existing integrity handling | Lifecycle status alone is insufficient for downstream automatic selection when historical pre-canon rows share the same status |
| 2026-09-01 | Milo Script Generator v0.1 | Historical `MILO-001-O01` reached Story readiness before eligible `MILO-007-O01`; handled failure stored `storyId=#NAME?` | Processed approved Outlines were passed downstream independently, so the first historical row could starve an eligible governed Story; the failure Set field was misspelled `stroyId` and used `=={{...}}` | Resolve all candidate Outlines to Stories, classify and select deterministically before downstream work; rename the field to `storyId` and use a single n8n expression prefix for both Story identity and status | Candidate-set selection must precede per-record workflow progression, and failure identities must be tested as literal Sheets values |
| 2026-09-01 | Milo Continuity Reviewer v0.1 | Historical `MILO-001-S01` reached Story readiness before eligible `MILO-007-S01` | Approved processed Scripts flowed independently into a first-item Story lookup, so an earlier lifecycle-ineligible historical candidate could starve the governed eligible pair | Read all candidate Stories once, resolve and classify the complete Script/Story set, deterministically select one governed `SCRIPT_APPROVED` pair, and only then continue duplicate Review and canon paths | Artifact approval alone is insufficient for automatic progression; candidate selection must include authoritative Story lifecycle and lineage eligibility |
