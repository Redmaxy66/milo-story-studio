# Visual Reference Generation Gate

**Status:** PROPOSAL PREPARED — EXECUTION NOT AUTHORISED

Phase 3 prepared reference specifications and approval structures only. It did not invoke OpenArt, ImageGen, the ChatGPT OpenArt integration, or any provider; did not create or upload reference images; did not spend credits; did not approve ungenerated assets; and did not treat spike media as a Milo reference.

A finite, minimum-viable proposal is now available at `reference-generation-proposal-v1.json`. It defines five reference sheets, one initial operation and at most one separately approved revision operation per sheet. The proposed OpenArt `TEXT_TO_IMAGE` route remains unverified. Current unit-cost evidence for that route is insufficient, so every per-sheet and total spending cap remains unset and blocks generation.

Before any request, a separate instruction must approve the provider target, current estimate, hard per-sheet and total caps, credentials, side effects, approval points, durable-storage plan, rollback, and stop conditions. The proposal does not approve any image, provider call, upload, retry, reference asset, or expenditure.

`PROPOSAL ONLY — NO OPENART ACCESS, GENERATION OR CREDIT SPEND AUTHORISED`
